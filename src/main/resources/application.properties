spring.application.name=backend

spring.datasource.url=jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:firstreps}
spring.datasource.username=${DB_USER:postgres}
spring.datasource.password=${DB_PASSWORD:postgres}

spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true

flyway.enabled=true
flyway.locations=classpath:db/migration

firstreps.jwt.secret=${JWT_SECRET:N5af5jtuWx8xKQnJSK11InsyRbNG0jBv}
firstreps.jwt.expiration-ms=3600000
firstreps.jwt.refresh-expiration-ms=604800000